<%- include('../partials/head', { title, description: '', ogImage: '' }) %>
<%- include('../partials/header') %>

<div class="container py-4">
  <h1>Branchen</h1>
  <p><a class="btn btn-primary" href="/admin/industries/new">+ Neue Branche</a></p>

  <table class="table table-striped align-middle">
    <thead><tr><th>Name</th><th>Slug</th><th>Bearbeitet</th><th></th></tr></thead>
    <tbody>
      <% rows.forEach(r => { %>
        <tr>
          <td><strong><%= r.name %></strong></td>
          <td><code><%= r.slug %></code></td>
          <td><%= new Date(r.updated_at).toLocaleString('de-DE') %></td>
          <td class="text-end">
            <a class="btn btn-sm btn-outline-secondary" href="/webdesign-<%= r.slug %>" target="_blank">Seite ansehen</a>
            <a class="btn btn-sm btn-primary" href="/admin/industries/<%= r.id %>/edit">Bearbeiten</a>
            <form method="post" action="/admin/industries/<%= r.id %>/id/delete" style="display:inline"></form>
            <form method="post" action="/admin/industries/<%= r.id %>/delete" style="display:inline" onsubmit="return confirm('Wirklich löschen?');">
              <button class="btn btn-sm btn-danger">Löschen</button>
            </form>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>

<%- include('../partials/footer') %>
