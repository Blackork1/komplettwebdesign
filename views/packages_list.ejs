<!DOCTYPE html>
<html lang="de">

<head>
  <%- include('partials/head') %> <!-- Annahme: Enthält Bootstrap CSS etc. -->
    <title>Unsere Website-Pakete</title>
</head>

<body>
  <%- include('partials/header') %>

    <div class="backgroundColor">
      <div class="topWhiteGradient">
        <div class="container py-5 my-0">
          <h1 class="mb-4 text-center white">Website-Pakete</h1>
          <p class="text-center mb-5">Wählen Sie das passende Paket für Ihr Webdesign-Projekt:</p>
          <div class="row">
            <% packages.forEach(pkg=> { if(pkg.display){%>
              <div class="col-md-4 mb-4">
                <div class="card h-100">
                  <!-- Optional: Paketbild als Thumbnail innerhalb der Karte -->
                  <img src="/images/<%= pkg.image %>" class="card-img-top" alt="<%= pkg.name %> Paket Bild">
                  <div class="card-body d-flex flex-column">
                    <!-- <h5 class="card-title"><%= pkg.name %>-Paket</h5> -->
                    <p class="card-text">
                      <%= pkg.description %>
                    </p>
                    <h6 class="mt-auto">Preis: <strong>
                        <%= pkg.price %> €
                      </strong></h6>
                    <a href="/pakete/<%= pkg.name.toLowerCase() %>" class="btn btn-primary mt-3">Details</a>
                  </div>
                </div>
              </div>
              <% }}) %>
          </div>
        </div>
      </div>
    </div>
    <%- include('partials/footer') %>
</body>

</html>